pipeline {
    agent { label 'main' } 
    stages {
        stage('Checkout') {
            steps {
                sh 'echo checkout step'
            }
        }
         stage('Testing') {
            steps {
                sh 'echo Testing step'
            }
        }
         stage('Print') {
            steps {
                sh 'echo print step'
            }
        }
    }
}
